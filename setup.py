from setuptools import find_packages, setup

setup(
    name = "src",
    version = "0.0.0.1",
    aurthor = "Abid",
    aurthor_emai = "abid.aslam55570@gmail.com",
    packages = find_packages(),
    install_requires = [],
)